CREATE TABLE  "CLIENTI" 
   (	"CODCLIENTE" NUMBER(5,0), 
	"CODCONFERMA" CHAR(16), 
	"TELEFONO" VARCHAR2(20) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(16) NOT NULL ENABLE, 
	"MAIL" VARCHAR2(50) NOT NULL ENABLE, 
	"VIA" VARCHAR2(60) NOT NULL ENABLE, 
	"CAP" NUMBER(5,0) NOT NULL ENABLE, 
	"SCALA" VARCHAR2(2), 
	"PIANO" NUMBER(3,0), 
	"CITTA" VARCHAR2(20) NOT NULL ENABLE, 
	"PROVINCIA" CHAR(2) NOT NULL ENABLE, 
	"PAESE" VARCHAR2(20) NOT NULL ENABLE, 
	"ATTIVO" NUMBER(1,0) NOT NULL ENABLE, 
	 CONSTRAINT "CLIENTI_PK" PRIMARY KEY ("CODCLIENTE") ENABLE
   ) ;

CREATE OR REPLACE TRIGGER  "BI_CLIENTI" 
  before insert on "CLIENTI"               
  for each row  
begin   
  if :NEW."CODCLIENTE" is null then 
    select "CLIENTI_SEQ".nextval into :NEW."CODCLIENTE" from dual; 
  end if; 
end; 

/
ALTER TRIGGER  "BI_CLIENTI" ENABLE;