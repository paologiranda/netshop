CREATE TABLE  "AZIENDE" 
   (	"PIVA" NUMBER(11,0) NOT NULL ENABLE, 
	"CODCLIENTE" NUMBER(5,0) NOT NULL ENABLE, 
	"RAGIONESOCIALE" VARCHAR2(20) NOT NULL ENABLE, 
	 CONSTRAINT "AZIENDE_PK" PRIMARY KEY ("PIVA") ENABLE
   ) ;ALTER TABLE  "AZIENDE" ADD CONSTRAINT "AZIENDE_FK" FOREIGN KEY ("CODCLIENTE")
	  REFERENCES  "CLIENTI" ("CODCLIENTE") ON DELETE CASCADE ENABLE;